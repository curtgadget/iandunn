# GitHub personal access token (classic) with the `read:user` scope.
# Used at build time to fetch the contribution graph — see README.md.
# Copy this file to `.env` and fill in the value. `.env` is git-ignored.
GITHUB_TOKEN=
